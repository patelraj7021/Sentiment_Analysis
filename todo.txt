analysis:
- integrate z-score comparisons instead of raw scores

data:
- build database so analysis can have enough data for means to be meaningful

UI:
- make list for tickers
- make 'enter' work as button press
- make app have scroll bars if window size is too low
- provide feedback on quick load and refresh separately
- provide last updated data time

scraping:
- make scraping start at random time within set window

docker:
- optimize image size
- include install for rust and npm

testing and documentation